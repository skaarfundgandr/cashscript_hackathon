{
  "name": "hermes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "bun run scripts/compile.ts",
    "dev": "bun run --filter '@hermes/shared' build && bun run --filter '@hermes/backend' dev",
    "dev:frontend": "bun run --filter '@hermes/shared' build && bun run --filter '@hermes/frontend' dev",
    "dev:shop": "bun run --filter '@hermes/shop-backend' dev",
    "dev:fixture": "bun run --filter '@hermes/shared' build && bun run --filter '@hermes/custody-fixture' dev",
    "dev:demo": "bun run dev:fixture & bun run dev:shop & bun run dev:frontend & wait",
    "dev:chipnet": "bash scripts/dev-chipnet.sh",
    "deploy": "docker compose up --build",
    "build": "bun run --filter '*' build"
  },
  "devDependencies": {
    "cashc": "^0.13.0",
    "cashscript": "^0.13.0",
    "typescript": "^5.7.0"
  }
}
